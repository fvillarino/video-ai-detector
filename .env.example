# Versión de la imagen publicada en GHCR a desplegar en el Beelink.
# Debe coincidir con un tag publicado (ver pestaña Packages del repo).
# Cambiá este valor y corré `make deploy` para actualizar el servicio.
#
# En el Beelink: cp .env.example .env  (y editá APP_VERSION)
APP_VERSION=v1.0.0
