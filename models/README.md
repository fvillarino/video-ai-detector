# models/

Este directorio es donde se almacenan los modelos YOLO descargados.

Al correr la aplicación por primera vez, `yolov8n.pt` se descargará automáticamente
desde los servers de Ultralytics y quedará cacheado aquí o en el directorio de trabajo.

No subir archivos `.pt` al repositorio (están en `.gitignore`).
